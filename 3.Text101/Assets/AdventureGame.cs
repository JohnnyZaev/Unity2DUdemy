using UnityEngine;
using UnityEngine.UI;

public class AdventureGame : MonoBehaviour
{
	public Text text;
    // Start is called before the first frame update
    void Start()
    {
	    text.text = "I change this text";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
